import { DefaultTheme } from 'styled-components'

const AppTheme: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },
}

export { AppTheme }
